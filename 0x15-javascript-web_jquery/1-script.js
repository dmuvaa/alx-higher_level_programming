// First, ensure that the jQuery library is loaded and the document is ready
$(document).ready(function() {
    // Use jQuery to select the <header> element and change its text color to red
    $("header").css("color", "#FF0000");
});

