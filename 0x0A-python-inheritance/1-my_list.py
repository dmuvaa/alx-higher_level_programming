#!/usr/bin/python3

"""Creates a class."""


def print_sorted(self):
    """"Class that that prints the list, but sorted (ascending sort)."""
    sorted_list = self.copy()
    sorted_list.sort()
    print(sorted_list)
